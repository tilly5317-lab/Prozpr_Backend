"""Value Research vendor-data mirror.

Isolated in its own Postgres schema (``vr``) with no foreign keys into the
application schema. See ``CLAUDE.md`` in this folder for the contract and the
rollout order.
"""

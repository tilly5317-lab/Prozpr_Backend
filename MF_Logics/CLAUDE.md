# MF_Logics/ — LEGACY

Historical mutual-fund data-extraction and mapping pipelines: `Mututal_Funds_data_extraction/` and `Mututal_Fund_Mapping_AA_Internal/`. Both are pre-production exploration work; active MF flows live under `app/services/` and `AI_Agents/src/`.

## Imported by active code?

NO

## When to touch this

Don't. If you're building a new MF pipeline, reference this for historical context only and write fresh code under `app/services/` or `AI_Agents/src/`.

## Don't read unless

- You want a historical mapping reference.

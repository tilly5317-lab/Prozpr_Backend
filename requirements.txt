# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0

# Database
sqlalchemy>=2.0.25
asyncpg>=0.30.0
aiosqlite>=0.20.0  # in-memory SQLite when USE_DATABASE=false (no Postgres)
alembic>=1.14.0

# Security
cryptography>=44.0.0
bcrypt>=4.0.0
PyJWT>=2.8.0

# HTTP client (MSG91 OTP)
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.1
python-multipart>=0.0.22
tzdata>=2024.1  # IANA time zones for zoneinfo (required on Windows; harmless elsewhere)
tavily

# AI
anthropic>=0.40.0
langchain-anthropic>=0.3.0
ddgs>=7.0.0

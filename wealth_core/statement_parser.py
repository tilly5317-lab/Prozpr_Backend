from typing import Dict


def parse_statement(raw_text: str) -> Dict[str, float]:
    """
    Parse raw statement text into asset class exposures or instrument-level map.
    For now, stub that returns empty dict.
    """
    return {}

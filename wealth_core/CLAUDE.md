# wealth_core/ — LEGACY

Pre-`app/` orchestration modules. Superseded by `app/services/` and `AI_Agents/src/`; retained for historical reference.

## Imported by active code?

NO

## When to touch this

Don't touch. If you need behavior similar to something here, implement fresh under `app/services/` or `AI_Agents/src/` rather than reviving legacy paths.

## Don't read unless

- You're investigating a historical implementation called out by name in git log or an issue.
